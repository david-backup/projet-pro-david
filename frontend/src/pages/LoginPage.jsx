import FormLogin from "../components/FormLogin";

const LoginPage = () => {
  return (
    <main>
      <section>
        <h1>Formulaire de connexion</h1>
        <FormLogin />
      </section>
    </main>
  );
};

export default LoginPage;
